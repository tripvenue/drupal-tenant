langcode: en
status: true
dependencies:
  config:
    - group.type.tenant
  module:
    - user
id: tenant-group_membership
label: 'Tenant: Group membership'
description: 'Adds users to groups as members.'
group_type: tenant
content_plugin: group_membership
plugin_config:
  group_cardinality: 1
  entity_cardinality: 1
